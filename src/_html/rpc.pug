doctype html
html
	head
		title RPC Test
		meta(charset="UTF-8")
		meta(name="viewport" content="width=device-width, initial-scale=1")
		link(rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.95em' font-size='90' x='-.1em'>🔗</text></svg>")
		script(type="module" src=(build?"/rpc.js":"/rpc.ts"))
		if build
			style: include ./rpc.min.css
		else
			link(rel="stylesheet" href="/_html/rpc.css")
	body
		label
			#autocomplete
			input(type="text" value="Rpc.getRegistrations()" autofocus)
		#buttonbar
			span#execute Execute
			span#copy Copy Result
			span#status Waiting for input
		pre#result
		if build
			script: include ./rpc.js
		else
			script(src="/_html/rpc.ts")